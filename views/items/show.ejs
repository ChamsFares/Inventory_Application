<!DOCTYPE html>
<html>
  <head>
    <title>Item Details</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
      }
      h1 {
        color: #333;
      }
      .item-details {
        margin-bottom: 20px;
      }
      .button {
        background-color: #007bff;
        color: white;
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        text-decoration: none;
      }
      .button:hover {
        background-color: #0056b3;
      }
      .back-link {
        margin-top: 20px;
        display: inline-block;
      }
    </style>
  </head>
  <body>
    <h1>Item Details</h1>
    <div class="item-details">
      <p><strong>Name:</strong> <%= item.name %></p>
      <p><strong>Description:</strong> <%= item.description %></p>
      <p><strong>Price:</strong> $<%= item.price.toFixed(2) %></p>
      <p><strong>Quantity:</strong> <%= item.quantity %></p>
      <p>
        <strong>Category:</strong> <%= category ? category.name : 'No category'
        %>
      </p>
    </div>
    <a href="/items/<%= item.id %>/edit" class="button">Edit Item</a>
    <form
      action="/items/<%= item.id %>/delete"
      method="POST"
      style="display: inline"
    >
      <button
        type="submit"
        class="button"
        style="background-color: #dc3545; border: none"
      >
        Delete Item
      </button>
    </form>
    <a href="/items" class="back-link">Back to Items</a>
  </body>
</html>
